# 1. 目的・背景（Rev.B）
Raspberry Pi 5 と SSD1306 搭載 OLED（I²C）を用いて、**日本語表示**を行う極めて入門的な Python サンプルを提供する。読者は**完全な初心者**を想定するため、学習ハードルを下げる設計とし、**機能は最小限**に絞る。ハードウェアは**双方向 I²C レベル変換モジュール常設**を前提とする。

- サンプル1：**simple_oled.py** … 日本語の固定表示（複数行）
- サンプル2：**scroll_oled.py** … 日本語文字列の横スクロール表示
- コメントスタイルはリポジトリの **COMMENT_STYLE_GUIDE.md** に準拠する。

> 変更点（Rev.B）：
> - 「13. 実行・操作方法（例）」を**削除**（書籍本文で操作手順を丁寧に解説）。
> - トラブル解決は**別冊ドキュメント**（`docs/TROUBLESHOOTING.md`）に集約する方針を追加。
> - 初心者配慮のため**設計を簡素化**（設定方法・抽象化層を縮小）。

---

# 2. 対象範囲（スコープ）
- **ハードウェア**：Raspberry Pi 5、SSD1306 I²C OLED モジュール（メーカー不問）、I²Cレベル変換（BSS138方式）
- **ソフトウェア**：Python 3.x、`luma.oled`（Pillowを内部利用）、`Pillow`
- **フォント**：商用利用可の日本語 TTF/OTF（例：Noto Sans CJK JP、M PLUS）
- **表示**：固定表示／横スクロール

# 3. 非対象
- SPI 接続、他ドライバ（SH1106 等）
- 画像・アイコン・複雑なアニメーション
- C/C++・Arduino 実装

# 4. ハードウェア要件
## 4.1 配線
- Pi I²C（GPIO2=SDA, GPIO3=SCL）を使用
- **レベル変換**：LV=3.3V（Pi側）、HV=OLED側電圧。GND共通
- I²C アドレス：**既定 0x3C**（必要に応じて 0x3D を説明）
- I²C クロック：標準 100 kHz を想定

## 4.2 機種差吸収の最小化
- 画面解像度（128×64 / 128×32）は**定数で切替**（サンプル内の先頭付近で変更）
- アドレスは**定数 `I2C_ADDRESS`** で変更（自動検出は行わない）

# 5. ソフトウェア構成・ライブラリ
- OS：Raspberry Pi OS（Bookworm 等）
- Python：3.11 以降推奨
- ライブラリ：`luma.oled`、`Pillow`
- **理由**：`luma.oled` は SSD1306 を簡単に扱え、`Pillow` の日本語フォント描画が容易。初心者でも理解しやすい最少APIで構成。

# 6. フォント（ライセンス）
- **OFL（商用利用可）** の日本語フォントを採用。
- 候補：Noto Sans CJK JP、M PLUS 1p
- リポジトリに `assets/fonts/` を作成し、フォントファイルと**ライセンス文書**を同梱。

# 7. 機能要件
## 7.1 共通
- UTF-8 の日本語文字列を表示。
- `Pillow` の `ImageFont.truetype()` でフォント読み込み。
- 画面初期化／クリア／描画反映（show）を提供。
- **初心者配慮**：
  - 設定は**コード先頭の定数**で行う（YAML等の外部設定は採用しない）。
  - 必須以外のオプションは**明示的に後半へ**（上級者向け欄）。

## 7.2 simple_oled.py（固定表示）
- 日本語の**複数行**を、**左寄せ**で表示（アライン固定）。
- 設定定数：
  - `TEXT_LINES = ["行1", "行2", ...]`
  - `FONT_PATH`, `FONT_SIZE`
  - `MARGIN_X`, `MARGIN_Y`, `LINE_SPACING`
  - `WIDTH`, `HEIGHT`, `I2C_ADDRESS`
- テキストが収まらない場合、**フォントサイズを調整して再実行**する運用を推奨（自動縮小は採用しない）。

## 7.3 scroll_oled.py（横スクロール）
- 1行テキストを右→左にスクロール。
- 設定定数：`SCROLL_TEXT`, `SCROLL_SPEED_PX`, `FRAME_DELAY_SEC`, フォント・解像度・マージン
- ループは**無限**を既定（`LOOP_COUNT` を指定した場合は回数で停止）。

# 8. エラーハンドリング・ログ
- 例外は簡潔に `print()` で通知（初心者向け）。
- 代表的エラー：I²C未検出、フォント未発見、権限不足（i2c 権限）
- **詳細な原因・解決策は** `docs/TROUBLESHOOTING.md` **に集約**（都度更新）。

# 9. 品質属性（目安）
- 固定表示：一度描画して安定表示。
- スクロール：20 FPS 相当（`FRAME_DELAY_SEC` の既定で適度な滑らかさ）
- I²C：100 kHz を既定（配線が短く安定しやすい）

# 10. セットアップ（最小）
- I²C 有効化（raspi-config）
- `pip install luma.oled pillow`
- フォントを `assets/fonts/` に配置

# 11. リポジトリ構成（簡素化版）
```
repo-root/
├─ src/
│  ├─ simple_oled.py
│  └─ scroll_oled.py
├─ assets/
│  └─ fonts/
│     └─ NotoSansCJKjp-Regular.otf など
├─ docs/
│  ├─ COMMENT_STYLE_GUIDE.md
│  └─ TROUBLESHOOTING.md   ← 本書の別冊トラブル解決集（新規作成）
├─ LICENSE
└─ README.md
```

# 12. 受入基準
- 128×64 / 128×32 いずれでも表示が崩れない（定数変更で対応）
- 文字化けしない（フォント未設置時は明確なエラー文言）
- スクロールが想定速度で動作（過度なカクつきなし）
- レベル変換使用・5V直結禁止の注意が README に明記

# 13. （削除）実行・操作方法（例）
※ Rev.Bでは章ごと削除。操作は書籍本文で図解解説。

# 14. リスクと対策
- **フォント容量**：CJK は大きい → 1書体/1ウェイトのみ採用。
- **I²C不安定**：配線長・二重プルアップ → 100 kHz 運用、GND共通。
- **互換性**：メーカー差 → 解像度・アドレスを定数化し最小の切替に限定。

# 15. ライセンス
- フォント：OFL 等の商用利用可ライセンスを同梱（`assets/fonts/` と `LICENSES/` で明示）
- サンプルコード：MIT（予定）

---

## 付録A：`docs/TROUBLESHOOTING.md`（新規作成・常時更新）
以下の雛形で**別ファイル**として作成・運用する。

```markdown
title: SSD1306 + Raspberry Pi 5 トラブルシューティング集

# 目的
本書のサンプル実行時に発生したエラーや不具合と、確認手順・解決策を**すべて**集約します。

# よくある症状と対処

## 1. `i2cdetect -y 1` にデバイスが表示されない
- **原因候補**: I²C無効化、配線誤り(SDA/SCL逆)、レベル変換LV/HV誤接続、電源未投入、アドレス相違(0x3C/0x3D)
- **確認**:
  - raspi-config で I²C 有効化、再起動
  - 配線図どおり再確認（SDA=GPIO2, SCL=GPIO3, GND共通）
  - レベル変換: LV=3.3V, HV=OLED側電圧
- **解決**: 配線修正、アドレス定数 `I2C_ADDRESS` を0x3C/0x3Dで切替

## 2. 実行時に `OSError: [Errno 121] Remote I/O error`
- **原因**: I²C通信失敗（上記1と同系）
- **解決**: 配線/I²C設定の再確認、ケーブル短縮、I²C 100kHz運用

## 3. 日本語が「□（豆腐）」になる／文字化け
- **原因**: フォント未配置、フォントに該当文字が無い
- **解決**: `assets/fonts/` に Noto Sans CJK JP 等を配置し、`FONT_PATH` を正しく設定

## 4. フォントが見つからない（`IOError: cannot open resource`）
- **原因**: パス誤り、権限不足
- **解決**: 絶対/相対パスの見直し、権限確認

## 5. スクロールがカクつく/速すぎる
- **原因**: フレーム間隔・速度設定不適切
- **解決**: `FRAME_DELAY_SEC` を大きく、`SCROLL_SPEED_PX` を小さく

## 6. 文字が画面からはみ出す
- **原因**: フォントサイズが大きい
- **解決**: `FONT_SIZE` を下げる（自動縮小はサンプル範囲外）

---

# 変更履歴
- yyyy-mm-dd: 初版作成
- yyyy-mm-dd: 事象Xを追記（原因・対策）
```

---

### 初心者配慮ポリシー（要点）
- 設定は**コード冒頭の定数**のみ（ファイル分割設定はしない）
- 既定値を**安全で動かしやすい**内容にする（0x3C, 128×64）
- 抽象化・多機能化は避け、**最小の読みやすいコード**を優先
- トラブル時は `docs/TROUBLESHOOTING.md` を参照し、**原因→確認→解決**の順で直せるようにする

---

# 16. 積み残し事項（次回対応）

## 16.1 複数OLED接続時の識別・制御

### 現状
- 本ハードウェアには2つのOLEDが接続されている
  1. **プロジェクト用OLED**: 今回接続したSSD1306モジュール
  2. **ケース付属OLED**: Raspberry Pi ステータス表示用（ケース組み込み）
- 両方のOLEDに同じ表示が出力される現象を確認

### 確認済み事項
- **I²Cバス構成**:
  - I2C-1 (GPIO I²C): アドレス 0x3C にデバイス1つ検出
  - I2C-13, I2C-14: 内部バス（異常応答）
- **現在の接続**: 両OLED が I2C-1 バス、アドレス 0x3C で接続されている可能性
- **識別スクリプト作成済み**: `src/test_oled_identify.py`

### 次回作業時の確認事項
1. **どちらのOLEDに表示されているか目視確認**
   ```bash
   cd src/
   python3 test_oled_identify.py
   ```
   - プロジェクト用OLEDのみ: OK（ケースOLEDは別アドレスまたは別バス）
   - ケース付属OLEDのみ: 配線を確認し、プロジェクト用に接続し直す
   - 両方に表示: 下記「対処方法」を実施

2. **ケースの型番・メーカー確認**
   - ケース付属OLEDの仕様・制御方法を調査
   - ステータス表示サービスの有無を確認

### 対処方法（候補）

#### 方法A: I²Cアドレスの変更（推奨）
- プロジェクト用OLEDのアドレスを **0x3C → 0x3D** に変更
- 多くのSSD1306モジュールには「ADDR」または「SA0」のジャンパー/ハンダパッドあり
- 変更後、スクリプト定数を修正:
  ```python
  I2C_ADDRESS = 0x3D  # プロジェクト用OLED
  ```
- ケース付属OLEDは 0x3C のまま（既存機能に影響なし）

#### 方法B: 別I²Cバスの使用
- Raspberry Pi 5 の別GPIOペアを使用（要 dtoverlay 設定）
- 配線変更が必要（初心者には難易度高）

#### 方法C: ケース付属OLEDの一時無効化
- 物理的に配線を外す
- または制御サービス/スクリプトを停止

### 影響範囲
- **現状**: サンプルスクリプトは正常動作（表示先が特定できていないのみ）
- **コード変更**: `I2C_ADDRESS` 定数の変更のみ（影響小）
- **ドキュメント**: 複数OLED環境のセットアップ手順を追加

### 優先度
- **中**: 基本機能は動作しているが、表示先が明確でないため、次回優先対応

### 備考
- 要件定義書 Rev.C で「複数OLED環境の対応」として正式化を検討

---


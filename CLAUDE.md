# Claude Code 作業ルール

## プロジェクト概要

このプロジェクトは、Raspberry Pi 5 と SSD1306 OLED (I²C) を使用した**日本語表示サンプル**の教育教材です。
**完全な初心者**を対象としているため、すべての設計・実装において**学習ハードルを下げること**を最優先します。

---

## 必須作業フロー

### 1. 作業開始時の確認事項

**必ず以下のドキュメントを確認してから作業を開始すること：**

1. **要件定義書**: `06-004-ssd_1306_oled_要件定義書（rev.md`
   - プロジェクトの目的、範囲、機能要件をすべて把握する
   - ハードウェア要件、ソフトウェア構成を理解する
   - 初心者配慮ポリシーを把握する

2. **コメントスタイルガイド**: `COMMENT_STYLE_GUIDE.md`
   - すべてのコードは本ガイドに準拠すること
   - docstring、インラインコメント、エラーメッセージの記載方法を確認

3. **既存コードの確認**:
   - 既存ファイルがある場合、スタイル・パターンを踏襲する

---

## 設計・実装の基本方針

### 初心者配慮（最優先事項）

- **設定方法**: すべての設定は**コード冒頭の定数**で行う
  - ❌ YAML、JSON、外部設定ファイルは使用しない
  - ✅ `I2C_ADDRESS = 0x3C` のような定数で設定

- **コードの簡素化**:
  - 抽象化層を最小限にする
  - クラス分割、モジュール分割は必要最小限
  - 読みやすさ > 拡張性

- **機能範囲**:
  - 必須機能のみ実装（スコープ外の機能は追加しない）
  - 多機能化は避ける

### コメント記載ルール

- **日本語**: すべてのコメントは日本語で記載
- **COMMENT_STYLE_GUIDE.md に厳密に準拠**:
  - ファイルヘッダー: シバン + docstring (目的、要件定義書への参照)
  - クラス docstring: 役割を簡潔に (3行以内)
  - メソッド docstring: 処理内容 + Args (型と説明)
  - インラインコメント: 処理の意図、初心者向け補足、トラブルシューティング情報
  - エラーメッセージ: 処理名 + ユーザー向け対処方法 + 具体的コマンド例

### エラーハンドリング

- **初心者向け**: 複雑なロギングライブラリは使わず `print()` で通知
- **エラーメッセージ形式**:
  ```python
  print(f"[処理名]エラー: {e}")
  print("対処方法: [具体的な手順]")
  print("ヒント: [コマンド例や確認方法]")
  ```
- **詳細な解決策**: `docs/TROUBLESHOOTING.md` に集約

---

## MCP ツール使用方針

### Google検索 (mcp__gemini-google-search__google_search)

- **用途**: Web検索が必要な場合に使用
- **活用場面**:
  - ライブラリの最新情報、ドキュメント確認
  - エラーメッセージの調査・解決策の検索
  - ベストプラクティスの確認
  - ライセンス情報の確認

### Serena（セマンティック分析ツール）

- **用途**: ドキュメントやコードの理解・把握に活用
- **活用場面**:
  - プロジェクト全体の構造分析
  - 複数ファイル間の関連性の理解
  - コードベースの概要把握
  - ドキュメント間の整合性確認

**重要**: これらのツールを積極的に活用し、正確な情報に基づいた実装を行うこと。

---

## プロジェクト構造

```
repo-root/
├─ src/
│  ├─ simple_oled.py      # 固定表示サンプル
│  └─ scroll_oled.py      # スクロール表示サンプル
├─ assets/
│  └─ fonts/              # 日本語フォント + ライセンス文書
│     └─ [NotoSansCJKjp-Regular.otf など]
├─ docs/
│  └─ TROUBLESHOOTING.md  # トラブルシューティング集
├─ COMMENT_STYLE_GUIDE.md
├─ CLAUDE.md              # 本ファイル
├─ LICENSE
└─ README.md
```

---

## 実装仕様

### simple_oled.py（固定表示）

**定数設定項目**:
- `TEXT_LINES = ["行1", "行2", ...]`: 表示する複数行テキスト
- `FONT_PATH`: フォントファイルパス
- `FONT_SIZE`: フォントサイズ（デフォルト: 12）
- `MARGIN_X`, `MARGIN_Y`: マージン
- `LINE_SPACING`: 行間隔
- `WIDTH`, `HEIGHT`: 画面解像度（128×64 または 128×32）
- `I2C_ADDRESS`: I²Cアドレス（デフォルト: 0x3C）

**処理フロー**:
1. I²C デバイス初期化 (`luma.oled`)
2. Pillow で画像生成 (ImageDraw)
3. 日本語フォント読み込み (`ImageFont.truetype()`)
4. 複数行テキストを左寄せで描画
5. OLED に表示 (`device.display()`)

**制約**:
- テキストが収まらない場合は**フォントサイズを手動で調整**（自動縮小は実装しない）

### scroll_oled.py（横スクロール）

**定数設定項目**:
- `SCROLL_TEXT`: スクロールする1行テキスト
- `SCROLL_SPEED_PX`: スクロール速度（ピクセル/フレーム）
- `FRAME_DELAY_SEC`: フレーム間隔（秒）
- `LOOP_COUNT`: ループ回数（None で無限ループ）
- フォント、解像度、I²Cアドレス（simple_oled.py と同様）

**処理フロー**:
1. デバイス初期化
2. 無限ループ（または指定回数）:
   - テキストを右→左にシフト
   - 画像を再描画
   - `time.sleep(FRAME_DELAY_SEC)` で待機

---

## ライブラリ仕様

### 使用ライブラリ

- **luma.oled**: SSD1306 制御
  ```python
  from luma.core.interface.serial import i2c
  from luma.oled.device import ssd1306
  ```
- **Pillow**: 日本語テキスト描画
  ```python
  from PIL import Image, ImageDraw, ImageFont
  ```

### インストールコマンド

```bash
pip install luma.oled pillow
```

---

## フォント要件

- **ライセンス**: OFL（商用利用可）
- **候補**: Noto Sans CJK JP、M PLUS 1p
- **配置場所**: `assets/fonts/`
- **ライセンス文書**: フォントと同じディレクトリに配置

---

## トラブルシューティング

### エラー発生時の対応

1. `docs/TROUBLESHOOTING.md` を確認
2. 新しいエラーが発生した場合:
   - 原因の特定
   - 確認手順の記載
   - 解決策の記載
   - `TROUBLESHOOTING.md` に追記

### よくあるエラー例

- `i2cdetect -y 1` にデバイスが表示されない → 配線、I²C設定、アドレス確認
- `OSError: [Errno 121] Remote I/O error` → I²C通信失敗（配線/設定の再確認）
- 日本語が「□（豆腐）」になる → フォント未配置、フォントパス誤り
- `IOError: cannot open resource` → フォントパス誤り、権限不足

---

## 品質基準（受入基準）

### 動作確認

- [ ] 128×64 および 128×32 の両解像度で正常表示
- [ ] 日本語が文字化けしない
- [ ] スクロールが滑らかに動作（過度なカクつきなし）
- [ ] フォント未設置時に明確なエラーメッセージが表示される

### コード品質

- [ ] `COMMENT_STYLE_GUIDE.md` に準拠
- [ ] 要件定義書の仕様をすべて満たす
- [ ] 初心者配慮ポリシーに沿っている
- [ ] エラーハンドリングが適切（print + ヒント）

### ドキュメント

- [ ] README.md にセットアップ手順、使用方法を記載
- [ ] レベル変換使用・5V直結禁止の注意を明記
- [ ] 要件定義書へのリンクを記載

---

## 開発時の注意事項

### ハードウェア

- **レベル変換必須**: Raspberry Pi 5 (3.3V) と OLED 間に BSS138 方式のレベル変換モジュールを使用
- **5V直結禁止**: Raspberry Pi のGPIOは3.3Vのため、5V OLEDの直結は厳禁
- **I²C設定**: `raspi-config` で I²C を有効化
- **I²Cクロック**: 100 kHz を既定（安定性優先）

### セキュリティ

- **フォントライセンス**: 必ずOFL等の商用利用可ライセンスを使用
- **ライセンス文書**: フォントと一緒に配布

---

## Git 運用

### コミットメッセージ

- 日本語または英語（プロジェクトの慣例に従う）
- 変更内容を簡潔に記載

### ブランチ戦略

- `main`: 安定版
- 新機能: フィーチャーブランチから PR

---

## コード開発ガイドラインへの準拠

本プロジェクトで新規作成するすべてのPythonコードは、以下の2つのガイドラインに**厳密に準拠**すること：

### 1. **COMMENT_STYLE_GUIDE.md**（コメント記載標準ガイド）

初心者向けコメント記載方法の標準化。以下を遵守すること：

- **ファイルヘッダー**: シバン + docstring（目的、要件定義書への参照）
- **クラスdocstring**: 役割を簡潔に（3行以内）
- **メソッド/関数docstring**: 処理内容 + Args（型と説明）
- **インラインコメント**: 処理の意図、初心者向け補足、トラブルシューティング情報
- **エラーメッセージ形式**: `[処理名]エラー: {e}` + 対処方法 + ヒント（具体的なコマンド例）

### 2. **python_coding_guidelines.md**（Pythonコーディングガイドライン）

初心者向けPythonコード開発の標準化。以下を遵守すること：

- **PEP 8準拠**: インデント4スペース、命名規則（snake_case/PascalCase/UPPER_CASE）
- **`if __name__ == "__main__":`の必須化**: 実行可能ファイル（20行以上）に必須
- **関数化**: グローバルに実行されるコードは関数に移動（setup → main → cleanup）
- **docstring**: すべての関数・クラスに記載（日本語、簡潔に1～3行）
- **エラーハンドリング**: 具体的な例外をキャッチし、初心者向けのメッセージを提示
- **インポート順序**: 標準ライブラリ → サードパーティ → 自作モジュール
- **ファイル構成順序**: シバン → docstring → インポート → 定数 → 関数定義 → `if __name__`

### チェックリスト（コード作成時）

新しいPythonコードを作成する際は、以下をすべて確認すること：

- [ ] COMMENT_STYLE_GUIDE.md に準拠したコメント記載
- [ ] python_coding_guidelines.md に準拠したコード構造
- [ ] ファイルヘッダーに要件定義書への参照がある
- [ ] 全関数にdocstring がある
- [ ] エラーハンドリングで「対処方法」と「ヒント」を提示
- [ ] `if __name__ == "__main__":` が含まれている（実行可能ファイルの場合）
- [ ] 定数がコード冒頭にまとめられている
- [ ] 初心者が理解できる説明的なコメントが付いている

---

## 参考資料

- **要件定義書**: `06-004-ssd_1306_oled_要件定義書（rev.md`
- **コメントガイド**: `COMMENT_STYLE_GUIDE.md`
- **Pythonコーディングガイド**: `python_coding_guidelines.md`
- **luma.oled ドキュメント**: https://luma-oled.readthedocs.io/
- **Pillow ドキュメント**: https://pillow.readthedocs.io/

---

**作業を開始する前に、必ず要件定義書・コメントスタイルガイド・Pythonコーディングガイドを読むこと！**
